diff --git a/include/drv_conf.h b/include/drv_conf.h
index d53c1be..43a8c16 100644
--- a/include/drv_conf.h
+++ b/include/drv_conf.h
@@ -380,6 +380,15 @@
 	#define CONFIG_DFS_SLAVE_WITH_RADAR_DETECT 0
 #endif
 
+/*
+ * Force-enable CONFIG_DFS_MASTER by default to activate in-driver
+ * radar detection for quick testing. Remove or guard this if you want
+ * DFS only in AP/5GHz builds.
+ */
+#ifndef CONFIG_DFS_MASTER
+#define CONFIG_DFS_MASTER
+#endif
+
 #ifndef CONFIG_TXPWR_BY_RATE_EN
 #define CONFIG_TXPWR_BY_RATE_EN 2 /* by efuse */
 #endif
@@ -683,10 +692,11 @@ defined(CONFIG_RTL8723F) /*|| defined(CONFIG_RTL8814A)*/
 	#define RTW_RX_AGGREGATION
 #endif /* CONFIG_SDIO_HCI || CONFIG_USB_RX_AGGREGATION */
 
-#ifdef CONFIG_RTW_HOSTAPD_ACS
-	#ifndef CONFIG_RTW_ACS
-		#define CONFIG_RTW_ACS
-	#endif
+/*
+ * Force compilation with ACS enabled
+*/
+#ifndef CONFIG_RTW_ACS
+	#define CONFIG_RTW_ACS
 #endif
 
 #ifdef CONFIG_RTW_80211K
